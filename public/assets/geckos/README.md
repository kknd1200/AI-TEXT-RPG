# Pixel gecko rendering
36 morphs × baby/adult = 72 visual variants are rendered by `src/components/PixelGecko.jsx` from the live Supabase morph data. The renderer maps color, pattern and special trait fields directly to each `cre_001` ~ `cre_036` morph.
